import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';






export const TermsOfService = () => {
    return (
        <View style={{backgroundColor: '#fff', height: '100%', width: '100%'}}>
            
        </View>
    )
}


const styles = StyleSheet.create({
    header: {backgroundColor: '#e6f0fa', height: 60, width: '100%', textAlign: 'center', alignItems: 'center',
    justifyContent: 'flex-end', position: 'relative', padding: 2},

    back: {position: 'absolute', left: 20, bottom: 5}
})